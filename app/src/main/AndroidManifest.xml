<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="agjs.gautham.rjsweets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".delivery.Dashboard" />
        <activity android:name=".admin.Dashboard" />
        <activity android:name=".user.Dashboard" />

        <activity android:name=".login.DeliveryLogin" />
        <activity android:name=".login.LoginAdmin" />
        <activity android:name=".login.Login" />

        <activity android:name=".MainActivity"/>

        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>